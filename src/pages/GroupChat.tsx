import GroupChatPageContent from '../components/groups/GroupChatPageContent';

/** Unified group chat — theme-aware via usePageContrast inside PageContent. */
export default function GroupChat() {
  return <GroupChatPageContent />;
}
