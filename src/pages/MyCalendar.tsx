import MyCalendarPageContent from '../components/calendar/MyCalendarPageContent';

/** Smart calendar (ZIP merge): theme-aware via usePageContrast; legacy theme files re-export this. */
export default function MyCalendar() {
  return <MyCalendarPageContent />;
}
