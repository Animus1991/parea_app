import CategoriesPageContent from '../components/categories/CategoriesPageContent';

/** Theme router keeps Classic entry; body is unified and uses usePageContrast. */
export default function CategoriesClassic() {
  return <CategoriesPageContent />;
}
