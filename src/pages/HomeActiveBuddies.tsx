/**
 * Dedicated Home for Active Buddies (light).
 * Layout and copy are shared with HomeClassic; visual tokens come from
 * `useHomeTheme()` + store theme `activebuddies`.
 */
export { default } from './HomeClassic';
