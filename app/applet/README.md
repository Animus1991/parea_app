# Legacy AI Studio applet repair scripts

One-off migration/repair tools from the original Google AI Studio export (`fix_jsx.cjs`, `restore.js`, etc.).

**Not used by the Vite build** (`npm run dev` / `build`). Safe to keep for reference; do not import from `src/`.

The maintained application lives under `src/` at the repository root.
